#include <bits/stdc++.h>
using namespace std;
int main()
{
    vector<int> arr;
    cout << arr.size();
    for (int i = 0; i < 10; i++)
    {
        arr.push_back(i);
    }
    for (int i = 0; i < arr.size(); i++)
    {
        cout << arr[i];
    }
    // cout<<arr.size();
    cout << arr.size();
    return 0;
}